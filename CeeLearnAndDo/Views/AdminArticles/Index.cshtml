@model IEnumerable<CeeLearnAndDo.Models.Article>

@{
    ViewBag.Title = "Artikelen-databank";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>Artikelen-databank</h2>

<p>
    @Html.ActionLink("Nieuwe artikel", "Create", null, new { @class = "btn btn-success" })
</p>
@*<table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Content)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ImagePath)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Approved)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreatedAt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UpdatedAt)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Title)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Content)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ImagePath)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Approved)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CreatedAt)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.UpdatedAt)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                </td>
            </tr>
        }

    </table>*@

@foreach (var item in Model)
{
    <div class="list-group">
        <a href="@Url.Action("Details", new { id = item.Id })" class="list-group-item list-group-item-action flex-column align-items-start">
            <div class="row">
                <div class="col-md-3">
                    <img class="card-img-top" src="~/UploadedFiles/ArticleImages/@Html.DisplayFor(modelItem => item.ImagePath)" />
                </div>
                <div class="col-md-9">
                    <div class="d-flex w-100 justify-content-between">
                        <h2 class="mb-1">@Html.DisplayFor(modelItem => item.Title)</h2>
                        <small style="text-align: right;">
                            @Html.DisplayFor(modelItem => item.UpdatedAt)<br/>
                            @{
                                if (item.Approved)
                                {
                                    <i>Goedgekeurd</i>
                                }
                                else
                                {
                                    <i>Nog niet goedgekeurd</i>
                                }
                            }
                        </small>
                    </div>
                    <p class="mb-1">
                        @if (item.Description.Length > 100)
                        {
                            @item.Description.Substring(0, 100)<span>...</span>
                        }
                        else
                        {
                            @Html.DisplayFor(modelItem => item.Description)
                        }
                    </p>


                </div>

            </div>

        </a>

    </div>
                        }